[project]
name = "bot-vinted-onepiece"
version = "0.1.0"
description = "Bot de surveillance des annonces Vinted One Piece"
authors = [ { name = "Vous" } ]
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "pydantic>=2.7.0",
    "rich>=13.7.0",
    "python-dotenv>=1.0.0"
]

[project.optional-dependencies]
notify = ["discord-webhook>=1.3.0"]

[tool.ruff]
line-length = 100
